name: Supabase keepalive

on:
  schedule:
    - cron: "0 0 */3 * *"   # 3日ごと（日本時間の朝9時）
  workflow_dispatch:         # 手動でも実行できる

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase
        run: |
          curl -fsS "https://wfixrtlthirberxzpsjc.supabase.co/rest/v1/scores?select=id&limit=1" \
            -H "apikey: sb_publishable_t0PU_FyCL3bh2K6fc4nNjA_vogH-oC7"
